#include "main.h"
#include <stdio.h>
#include <stdlib.h>
/*
 *
 *
 *
 */
void print_dog(struct dog *d)
{
struct dog my_dog(char *name, float age, char *owner);
d = (struct)malloc(sizeof(struct dog));
d = &my_dog;
d -> name = my_dog.name;
d -> age = my_dog.age;
d -> owner = my_dog.owner
if (d == NULL)
printf("nil");
if (name == NULL )
printf("Name: nil");
if (d == NULL)
return;
}
