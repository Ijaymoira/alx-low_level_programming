!<arch>
0-isupper.c/    0           0     0     644     377       `
#include "main.h"
#include <stdio.h>
/**
 * create_array - function that creates an array of chars
 * and initializes it with a specific char.
 * @c: initialized chairacter in array
 * @size: the size of the memory
 * Return: Returns NULL if size = 0
 * or pointer to the array, or NULL if it fails
 * main - initializing the create_array function
 */
int _isupper(int c)
{

}

0-memset.c/     0           0     0     644     403       `
#include "main.h"
#include <stdio.h>
/**
 * create_array - function that creates an array of chars
 * and initializes it with a specific char.
 * @c: initialized chairacter in array
 * @size: the size of the memory
 * Return: Returns NULL if size = 0
 * or pointer to the array, or NULL if it fails
 * main - initializing the create_array function
 */
char *_memset(char *s, char b, unsigned int n)
{
}

0-strcat.c/     0           0     0     644     393       `
#include "main.h"
#include <stdio.h>
/**
 * create_array - function that creates an array of chars
 * and initializes it with a specific char.
 * @c: initialized chairacter in array
 * @size: the size of the memory
 * Return: Returns NULL if size = 0
 * or pointer to the array, or NULL if it fails
 * main - initializing the create_array function
 */
char *_strcat(char *dest, char *src)
{
}

100-atoi.c/     0           0     0     644     375       `
#include "main.h"
#include <stdio.h>
/**
 * create_array - function that creates an array of chars
 * and initializes it with a specific char.
 * @c: initialized chairacter in array
 * @size: the size of the memory
 * Return: Returns NULL if size = 0
 * or pointer to the array, or NULL if it fails
 * main - initializing the create_array function
 */
int _atoi(char *s)
{
}

1-isdigit.c/    0           0     0     644     376       `
#include "main.h"
#include <stdio.h>
/**
 * create_array - function that creates an array of chars
 * and initializes it with a specific char.
 * @c: initialized chairacter in array
 * @size: the size of the memory
 * Return: Returns NULL if size = 0
 * or pointer to the array, or NULL if it fails
 * main - initializing the create_array function
 */
int _isdigit(int c)
{
}
1-memcpy.c/     0           0     0     644     409       `
#include "main.h"
#include <stdio.h>
/**
 * create_array - function that creates an array of chars
 * and initializes it with a specific char.
 * @c: initialized chairacter in array
 * @size: the size of the memory
 * Return: Returns NULL if size = 0
 * or pointer to the array, or NULL if it fails
 * main - initializing the create_array function
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
}

1-strncat.c/    0           0     0     644     401       `
#include "main.h"
#include <stdio.h>
/**
 * create_array - function that creates an array of chars
 * and initializes it with a specific char.
 * @c: initialized chairacter in array
 * @size: the size of the memory
 * Return: Returns NULL if size = 0
 * or pointer to the array, or NULL if it fails
 * main - initializing the create_array function
 */
char *_strncat(char *dest, char *src, int n)
{
}

2-strchr.c/     0           0     0     644     387       `
#include "main.h"
#include <stdio.h>
/**
 * create_array - function that creates an array of chars
 * and initializes it with a specific char.
 * @c: initialized chairacter in array
 * @size: the size of the memory
 * Return: Returns NULL if size = 0
 * or pointer to the array, or NULL if it fails
 * main - initializing the create_array function
 */
char *_strchr(char *s, char c)
{
}

2-strlen.c/     0           0     0     644     377       `
#include "main.h"
#include <stdio.h>
/**
 * create_array - function that creates an array of chars
 * and initializes it with a specific char.
 * @c: initialized chairacter in array
 * @size: the size of the memory
 * Return: Returns NULL if size = 0
 * or pointer to the array, or NULL if it fails
 * main - initializing the create_array function
 */
int _strlen(char *s)
{
}

2-strncpy.c/    0           0     0     644     401       `
#include "main.h"
#include <stdio.h>
/**
 * create_array - function that creates an array of chars
 * and initializes it with a specific char.
 * @c: initialized chairacter in array
 * @size: the size of the memory
 * Return: Returns NULL if size = 0
 * or pointer to the array, or NULL if it fails
 * main - initializing the create_array function
 */
char *_strncpy(char *dest, char *src, int n)
{
}

3-islower.c/    0           0     0     644     376       `
#include "main.h"
#include <stdio.h>
/**
 * create_array - function that creates an array of chars
 * and initializes it with a specific char.
 * @c: initialized chairacter in array
 * @size: the size of the memory
 * Return: Returns NULL if size = 0
 * or pointer to the array, or NULL if it fails
 * main - initializing the create_array function
 */
int _islower(int c)
{
}
3-puts.c/       0           0     0     644     376       `
#include "main.h"
#include <stdio.h>
/**
 * create_array - function that creates an array of chars
 * and initializes it with a specific char.
 * @c: initialized chairacter in array
 * @size: the size of the memory
 * Return: Returns NULL if size = 0
 * or pointer to the array, or NULL if it fails
 * main - initializing the create_array function
 */
void _puts(char *s)
{
}
3-strcmp.c/     0           0     0     644     388       `
#include "main.h"
#include <stdio.h>
/**
 * create_array - function that creates an array of chars
 * and initializes it with a specific char.
 * @c: initialized chairacter in array
 * @size: the size of the memory
 * Return: Returns NULL if size = 0
 * or pointer to the array, or NULL if it fails
 * main - initializing the create_array function
 */
int _strcmp(char *s1, char *s2)
{
}
3-strspn.c/     0           0     0     644     400       `
#include "main.h"
#include <stdio.h>
/**
 * create_array - function that creates an array of chars
 * and initializes it with a specific char.
 * @c: initialized chairacter in array
 * @size: the size of the memory
 * Return: Returns NULL if size = 0
 * or pointer to the array, or NULL if it fails
 * main - initializing the create_array function
 */
unsigned int _strspn(char *s, char *accept)
{
}
4-isalpha.c/    0           0     0     644     377       `
#include "main.h"
#include <stdio.h>
/**
 * create_array - function that creates an array of chars
 * and initializes it with a specific char.
 * @c: initialized chairacter in array
 * @size: the size of the memory
 * Return: Returns NULL if size = 0
 * or pointer to the array, or NULL if it fails
 * main - initializing the create_array function
 */

int _isalpha(int c)
{
}

4-strpbrk.c/    0           0     0     644     394       `
#include "main.h"
#include <stdio.h>
/**
 * create_array - function that creates an array of chars
 * and initializes it with a specific char.
 * @c: initialized chairacter in array
 * @size: the size of the memory
 * Return: Returns NULL if size = 0
 * or pointer to the array, or NULL if it fails
 * main - initializing the create_array function
 */
char *_strpbrk(char *s, char *accept)
{
}
5-strstr.c/     0           0     0     644     400       `
#include "main.h"
#include <stdio.h>
/**
 * create_array - function that creates an array of chars
 * and initializes it with a specific char.
 * @c: initialized chairacter in array
 * @size: the size of the memory
 * Return: Returns NULL if size = 0
 * or pointer to the array, or NULL if it fails
 * main - initializing the create_array function
 */
char *_strstr(char *haystack, char *needle)
{
}
6-abs.c/        0           0     0     644     372       `
#include "main.h"
#include <stdio.h>
/**
 * create_array - function that creates an array of chars
 * and initializes it with a specific char.
 * @c: initialized chairacter in array
 * @size: the size of the memory
 * Return: Returns NULL if size = 0
 * or pointer to the array, or NULL if it fails
 * main - initializing the create_array function
 */
int _abs(int n)
{
}
9-strcpy.c/     0           0     0     644     393       `
#include "main.h"
#include <stdio.h>
/**
 * create_array - function that creates an array of chars
 * and initializes it with a specific char.
 * @c: initialized chairacter in array
 * @size: the size of the memory
 * Return: Returns NULL if size = 0
 * or pointer to the array, or NULL if it fails
 * main - initializing the create_array function
 */
char *_strcpy(char *dest, char *src)
{
}

_putchar.c/     0           0     0     644     377       `
#include "main.h"
#include <stdio.h>
/**
 * create_array - function that creates an array of chars
 * and initializes it with a specific char.
 * @c: initialized chairacter in array
 * @size: the size of the memory
 * Return: Returns NULL if size = 0
 * or pointer to the array, or NULL if it fails
 * main - initializing the create_array function
 */
int _putchar(char c)
{
}

