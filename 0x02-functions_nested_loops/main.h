/**
 * main entry point
 *
 * Description - prototype and function for string.
 *
 * Return:Always 0 (Success)
 */
void first_strin(char *s)
{
int i = 0;
while (s[i] != '\0')
{
putchar(s[i]);
i++;
}
}
